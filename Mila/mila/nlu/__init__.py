"""NLU: fuzzy matching, Hebrew phonetics, rule-based intent classification.

Accepts noisy child input (typed in the skeleton; ASR-derived later) and
produces an `Intent` plus candidate `LexEntry` matches.
"""
